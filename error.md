错误日志1

```
Error:(100, 12) not enough arguments for method apply: (index: Int)Char in class StringOps.
Unspecified value parameter index.
 a.getName()
注意：如果定义时的def后的方法名没加()，在调用的时候就不能加();定义def的方法名加了()调用时，可以加()也可以不加()

```

